# 实施计划：JoJo 命运引力基础版

## 使用说明

这份计划面向 AI 开发者。执行任何代码生成或修改前，必须先完整阅读：

- `memory-bank/@architecture.md`
- `memory-bank/@game-design-document.md`
- `memory-bank/@tech-stack.md`

本计划只聚焦基础功能。目标是先交付一个稳定、可用、可测试的 2D 人物关系图谱网站。完整角色库、复杂动画、后端、登录、用户投稿、3D 展示和多人功能都不进入第一阶段。

严禁把所有功能写进一个单体巨文件。每一步都要保持模块化，多文件拆分，遵守 `.cursor/rules` 中的 Always 规则。

## 阶段 1：项目初始化与基础约束

### 步骤 1：初始化 Vite React TypeScript 项目

指令：

- 使用 Vite 创建 React + TypeScript 前端项目。
- 保持项目为静态 SPA。
- 不引入 Next.js、后端服务、数据库、WebSocket 或 Three.js。
- 初始化完成后，确认项目根目录仍保留 `memory-bank`、`.cursor/rules`、`game-design-document.md`、`tech-stack.md` 和本计划文件。

验证测试：

- 运行依赖安装。
- 运行本地开发服务器，确认默认页面能打开。
- 运行生产构建，确认没有 TypeScript 或构建错误。

### 步骤 2：建立基础目录结构

指令：

- 按 `memory-bank/@architecture.md` 建立 `src/app`、`src/components`、`src/data`、`src/domain`、`src/hooks`、`src/styles`、`src/tests`。
- 不要在此步骤实现业务功能。
- 创建空模块或最小占位文件时，只表达职责边界，不写复杂逻辑。
- 确保 `App` 只负责应用组合，不承担图谱、数据转换、筛选和详情逻辑。

验证测试：

- 检查目录结构是否与架构文档一致。
- 运行构建，确认新增目录和占位模块不破坏项目。
- 人工检查没有出现单体巨文件或过早混合职责的文件。

### 步骤 3：接入全局样式和设计 tokens

指令：

- 创建全局基础样式和设计 tokens。
- 使用游戏设计文档中的主色、文字色、强调色、关系色和中性色。
- 全局样式只放基础排版、根布局、颜色变量和最小 reset。
- 组件样式后续使用 CSS Modules，不要创建一个覆盖所有组件的大型全局样式表。

验证测试：

- 运行应用，确认页面背景、文字颜色和基础布局生效。
- 检查全局样式文件只包含全局职责，没有组件私有样式。
- 运行构建，确认样式导入无错误。

## 阶段 2：数据模型与验证

### 步骤 4：定义领域类型

指令：

- 在领域模块中定义 Character、Relationship、Collectible、FilterState、ActiveView 等类型。
- 类型必须匹配 `tech-stack.md` 和 `memory-bank/@architecture.md` 中的数据模型。
- 不要把类型定义散落到组件文件里。

验证测试：

- 运行 TypeScript 类型检查或生产构建。
- 人工检查组件目录中没有重复定义核心领域类型。
- 确认角色、关系和收藏物类型支持 MVP 所需字段。

### 步骤 5：创建最小静态数据集

指令：

- 在 `src/data` 中创建角色、关系和收藏物数据。
- 数据应拆分为 `characters`、`relationships`、`collectibles` 三类。
- 首批数据聚焦 Part 1、Part 3、Part 7 的核心角色。
- 至少包含乔纳森、迪奥、承太郎、乔瑟夫、乔鲁诺、杰洛、乔尼。
- 至少包含血亲、敌对、同伴、师承、命运呼应五类关系中的基础样例。
- 不要一次性录入完整 Part 1-9 数据。

验证测试：

- 人工检查每个角色都有稳定唯一 ID。
- 人工检查每条关系的 source 和 target 都能对应到已有角色。
- 运行构建，确认 JSON 或数据导入没有格式错误。

### 步骤 6：加入 Zod 数据校验

指令：

- 为角色、关系和收藏物建立 Zod schema。
- 应用启动时对静态数据做校验。
- 校验失败时给开发者可读的错误信息，不要让页面静默失败。
- 将校验逻辑放在领域模块中，不要写在 UI 组件中。

验证测试：

- 编写单元测试验证合法角色数据能通过校验。
- 编写单元测试验证缺少必要字段的数据会被拒绝。
- 编写单元测试验证关系引用不存在角色时会被识别为错误。

## 阶段 3：应用壳与基础布局

### 步骤 7：实现应用主布局

指令：

- 创建主应用壳，包括顶部工具栏区域、主图谱区域、右侧详情面板区域和底部状态区域。
- 桌面端采用主图谱加右侧详情面板。
- 移动端预留底部抽屉式详情面板空间。
- 先完成布局结构，不实现复杂交互。

验证测试：

- 在桌面宽度打开页面，确认图谱区域占据主要空间，详情面板在右侧。
- 在移动宽度打开页面，确认布局不横向溢出。
- 检查页面没有文字重叠、按钮溢出或不可见区域。

### 步骤 8：实现顶部筛选栏占位

指令：

- 创建 FilterBar 组件。
- 包含搜索输入、部数筛选、关系类型筛选和重置按钮的位置。
- 此步骤只完成可见 UI 和基础无障碍标签。
- 筛选逻辑在后续步骤实现。

验证测试：

- 运行页面，确认顶部筛选栏可见且不遮挡图谱区域。
- 使用键盘 Tab，确认输入框和按钮可以聚焦。
- 在移动宽度检查筛选栏不会导致横向滚动。

### 步骤 9：实现详情面板空状态

指令：

- 创建 CharacterPanel 组件。
- 在未选中角色时显示简洁空状态。
- 详情面板不要包含图谱逻辑或数据筛选逻辑。
- 空状态文案应提示用户选择角色，但不要写成营销介绍页。

验证测试：

- 打开页面时确认详情面板显示空状态。
- 检查 CharacterPanel 文件只负责详情显示，不包含 Cytoscape 初始化或筛选逻辑。
- 在桌面和移动宽度确认空状态内容不溢出。

## 阶段 4：图谱基础渲染

### 步骤 10：安装并隔离 Cytoscape.js

指令：

- 安装 Cytoscape.js。
- 创建 GraphCanvas 组件，集中管理 Cytoscape 实例生命周期。
- 不要让其他组件直接操作 Cytoscape 实例。
- 暂时只渲染容器和最小图谱，不实现筛选和详情联动。

验证测试：

- 运行页面，确认图谱容器能正常挂载。
- 打开浏览器控制台，确认没有重复初始化或销毁错误。
- 运行构建，确认 Cytoscape 依赖能被正常打包。

### 步骤 11：建立图谱数据转换模块

指令：

- 在领域模块中创建数据到 Cytoscape 元素的转换逻辑。
- 节点元素来自角色数据。
- 边元素来自关系数据。
- 转换模块不得依赖 React 组件。

验证测试：

- 编写单元测试确认每个角色会生成一个节点元素。
- 编写单元测试确认每条关系会生成一条边元素。
- 编写单元测试确认节点和边携带详情面板需要的基础 ID 和标签。

### 步骤 12：渲染基础关系图谱

指令：

- 将经过校验和转换的数据传入 GraphCanvas。
- 使用 Cytoscape 渲染节点和关系线。
- 使用一个稳定的基础布局，让 MVP 数据集清晰可见。
- 支持平移和缩放。

验证测试：

- 打开页面，确认能看到角色节点和关系线。
- 使用鼠标或触控板测试缩放和平移。
- 人工检查节点数量和关系数量与静态数据一致。
- 运行构建，确认无错误。

### 步骤 13：加入基础图谱样式

指令：

- 将 Cytoscape 样式放入独立图谱样式模块。
- 按设计文档区分普通角色、Joestar 血脉、主要反派和选中态。
- 按关系类型区分线条颜色或线型。
- 不要只依赖颜色区分关系类型，至少预留线型或标签规则。

验证测试：

- 人工检查不同角色类型在图谱中有可辨识样式。
- 人工检查不同关系类型不是完全相同的视觉表现。
- 在深色背景下检查节点标签可读。

## 阶段 5：选择角色与详情展示

### 步骤 14：建立选中状态 Hook

指令：

- 创建管理当前选中角色 ID 的 Hook 或 Context。
- 选中状态应能被 GraphCanvas 和 CharacterPanel 共享。
- 不要将选中状态写死在 GraphCanvas 内部。

验证测试：

- 编写单元测试或组件测试确认设置选中 ID 后能读取到同一 ID。
- 人工检查状态逻辑不依赖具体 UI 组件。
- 运行构建，确认类型无误。

### 步骤 15：实现点击节点选中角色

指令：

- 在 GraphCanvas 中监听节点点击事件。
- 点击节点后更新选中角色 ID。
- 高亮当前选中节点，并弱化无关节点的样式预留到后续增强。

验证测试：

- 手动点击每个基础角色节点，确认选中状态变化。
- 打开控制台确认点击不会产生运行时错误。
- 编写 Playwright 冒烟测试的一部分：点击节点后页面进入选中状态。

### 步骤 16：展示角色详情

指令：

- CharacterPanel 根据选中角色 ID 展示角色姓名、英文名、部数、家族、替身、经典台词、简介和标签。
- 没有替身的角色应显示清晰的空值状态，而不是报错。
- 详情展示逻辑不得修改图谱实例。

验证测试：

- 点击乔纳森，确认详情显示其资料且替身为空值状态正常。
- 点击承太郎，确认详情显示替身资料。
- 编写组件测试或 Playwright 测试确认点击节点后详情面板出现对应姓名。

## 阶段 6：搜索与筛选

### 步骤 17：实现搜索匹配逻辑

指令：

- 在领域模块中实现搜索匹配逻辑。
- 搜索应支持中文名、英文名或罗马音字段。
- 搜索逻辑不要写在输入框组件里。

验证测试：

- 编写单元测试确认中文名能匹配角色。
- 编写单元测试确认英文名或罗马音能匹配角色。
- 编写单元测试确认无匹配时返回空结果。

### 步骤 18：接入搜索输入

指令：

- 将 FilterBar 的搜索输入连接到搜索状态。
- 输入搜索词后，图谱应高亮匹配角色或聚焦到匹配结果。
- 初版可只处理第一个匹配结果。

验证测试：

- 手动输入“迪奥”，确认图谱中迪奥节点被定位或高亮。
- 手动输入不存在的角色名，确认页面给出无结果状态或不崩溃。
- 编写 Playwright 测试：输入角色名后对应详情或节点状态可被验证。

### 步骤 19：实现部数筛选逻辑

指令：

- 在领域模块中实现按 Part 筛选角色和关系。
- 筛选后只显示相关节点和有效关系。
- 不要在 GraphCanvas 中直接写筛选算法。

验证测试：

- 编写单元测试确认选择 Part 1 时只返回 Part 1 相关角色和关系。
- 编写单元测试确认跨部角色在对应 Part 中能被保留。
- 手动筛选 Part 3，确认图谱更新且无孤立错误边。

### 步骤 20：实现关系类型筛选逻辑

指令：

- 在领域模块中实现按关系类型筛选。
- 支持至少血亲、敌对、同伴、师承、命运呼应。
- 筛选关系后，应保留这些关系两端需要显示的角色。

验证测试：

- 编写单元测试确认选择敌对关系时只返回敌对边。
- 编写单元测试确认敌对边两端角色仍然存在。
- 手动切换关系筛选，确认图谱边数量和类型明显变化。

### 步骤 21：接入筛选栏控件

指令：

- 将部数筛选和关系类型筛选连接到共享筛选状态。
- 图谱根据筛选状态更新显示。
- 重置按钮恢复默认全局视图。

验证测试：

- 手动选择 Part 1，确认图谱只显示相关内容。
- 手动选择敌对关系，确认关系线更新。
- 点击重置，确认图谱恢复基础全局视图。
- 编写 Playwright 测试覆盖筛选和重置流程。

## 阶段 7：图例、状态栏与基础收藏

### 步骤 22：实现图例组件

指令：

- 创建 Legend 组件。
- 解释节点形态、关系线颜色、线型和选中态。
- 图例应保持简洁，不遮挡核心图谱操作。

验证测试：

- 桌面端检查图例可见且不覆盖详情面板。
- 移动端检查图例可以折叠或位于不阻碍操作的位置。
- 人工确认图例描述与实际图谱样式一致。

### 步骤 23：实现底部状态栏

指令：

- 创建底部状态栏。
- 显示当前节点数量、关系数量、当前视图或筛选摘要。
- 状态栏只读取状态，不承担筛选计算逻辑。

验证测试：

- 默认视图下确认节点和关系数量正确。
- 应用筛选后确认状态栏数量随图谱结果变化。
- 在移动端确认状态栏文字不溢出。

### 步骤 24：实现收藏物数据和触发逻辑

指令：

- 在领域模块中实现圣者遗体收藏触发判断。
- 首版至少实现 3 个隐藏收藏触发点。
- 触发逻辑应基于角色选择、关系展开或视图切换等明确事件。
- 不要把收藏判断写在 UI 渲染函数中。

验证测试：

- 编写单元测试确认选择指定角色可以触发指定收藏物。
- 编写单元测试确认无关操作不会触发收藏物。
- 编写单元测试确认重复触发不会重复计数。

### 步骤 25：实现收藏进度显示

指令：

- 创建 CollectiblesBar 组件。
- 显示 9 块圣者遗体的发现进度。
- 已发现和未发现状态要清晰区分。
- 首版使用本地状态或 `localStorage` 保存发现进度。

验证测试：

- 手动触发一个收藏物，确认进度增加。
- 刷新页面，确认已发现状态仍然保留。
- 清除本地存储后刷新，确认进度回到初始状态。

## 阶段 8：响应式与可访问性

### 步骤 26：完善桌面布局

指令：

- 调整桌面端图谱、详情面板、筛选栏、图例和状态栏的尺寸关系。
- 主图谱应始终是第一视觉重点。
- 不要让卡片层层嵌套。

验证测试：

- 在常见桌面宽度检查布局。
- 确认详情面板不会挤压图谱到不可用。
- 检查无文本重叠和按钮溢出。

### 步骤 27：完善移动布局

指令：

- 移动端默认保留可操作图谱。
- 详情面板改为底部抽屉或可折叠面板。
- 筛选入口在移动端应紧凑，不造成横向滚动。

验证测试：

- 在移动宽度打开页面，确认没有横向滚动。
- 点击节点后确认详情可见且不完全遮挡图谱。
- 使用搜索和筛选控件，确认触控目标足够可用。

### 步骤 28：加入基础键盘和无障碍支持

指令：

- 确保搜索、筛选、重置、图例开关和详情面板操作可键盘访问。
- 图标按钮必须有可读标签。
- 关系类型不能只用颜色表达。
- 动效要尊重 reduced motion。

验证测试：

- 只使用键盘完成搜索、筛选和重置。
- 使用浏览器无障碍检查工具检查明显 label 缺失。
- 开启 reduced motion 后确认动效减少或被替代。

## 阶段 9：基础测试与交付检查

### 步骤 29：补齐单元测试

指令：

- 为数据校验、搜索、部数筛选、关系类型筛选、收藏触发和图谱元素转换补齐 Vitest 测试。
- 测试应覆盖正常输入、空输入和错误输入。
- 不要只测试组件是否能渲染，要重点测试领域逻辑。

验证测试：

- 运行全部 Vitest 测试，确认全部通过。
- 人工检查测试覆盖了核心基础功能。
- 故意检查一个无效数据用例，确认测试能证明校验有效。

### 步骤 30：补齐 Playwright 冒烟测试

指令：

- 创建端到端冒烟测试。
- 测试流程应覆盖：打开网站、搜索角色、点击节点、查看详情、使用筛选、重置筛选。
- 增加一个移动端视口测试，确认基础布局可用。

验证测试：

- 运行 Playwright 测试，确认桌面流程通过。
- 运行 Playwright 移动视口测试，确认没有关键交互失败。
- 检查测试失败截图或报告，确认没有明显遮挡或文本溢出。

### 步骤 31：执行最终人工验收

指令：

- 对照 `memory-bank/@game-design-document.md` 的验收标准进行检查。
- 确认用户能在 10 秒内理解这是 JoJo 人物关系图谱。
- 确认用户能搜索角色、点击节点、查看详情、区分关系类型。
- 确认至少 3 个收藏触发点可用。

验证测试：

- 手动完成一次完整用户旅程：进入页面、搜索角色、点击节点、筛选关系、触发收藏、重置视图。
- 桌面和移动端各完成一次验收。
- 记录所有未完成项，并只把阻塞基础功能的问题放入当前阶段修复。

### 步骤 32：更新架构文档

指令：

- 完成基础版后，更新 `memory-bank/@architecture.md`。
- 记录真实目录结构、已采用依赖、数据结构、状态结构、测试策略和任何偏离原计划的技术决策。
- 不要让架构文档停留在初始设想。

验证测试：

- 人工检查架构文档与实际项目结构一致。
- 人工检查新增依赖都能在架构文档中找到理由。
- 确认 `.cursor/rules/000-always-read-memory-bank.mdc` 的里程碑更新要求已被满足。

## 后续阶段暂缓

以下内容等基础版稳定后再计划：

- Part 1-9 完整角色库。
- 更复杂的黄金旋回动画。
- 二级关系网展开的高级布局。
- 用户自定义图谱布局。
- 管理后台或投稿系统。
- 云端收藏同步。
- 3D 圣者遗体陈列。
- 社区、评论或登录。

